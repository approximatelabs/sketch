# this folder is a series of streamlit examples
# that are built on the component methods / APIs
# found in the sketch/api/templates/components jinja templates
# which are directly called via a specific API endpoint

# can also just include basic data examples
